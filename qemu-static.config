#!/bin/sh
# Generated by configure.
# Run this file to recreate the current configuration.
# Compiler output produced by configure, useful for debugging
# configure, is in config.log if it exists.
exec '../configure' '--target-list=arm-softmmu' '--audio-drv-list=' '--static' '--disable-nettle' '--disable-netmap' '--disable-vhost-net' '--disable-slirp' '--disable-vde' '--disable-usb-redir' '--disable-nettle' '--disable-gnutls' '--disable-gcrypt' '--disable-vhost-vsock' '--disable-sdl' '--disable-gtk' '--disable-vte' '--disable-curses' '--disable-vnc-sasl' '--disable-vnc-jpeg' '--disable-vnc' "$@"
